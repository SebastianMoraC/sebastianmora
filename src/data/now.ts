export interface NowEntry {
  building: string;
  reading: string;
  listening: string;
  learning: string;
}

export const now: NowEntry = {
  building: 'An AI quote-generation platform with multi-agent reasoning over technical blueprints.',
  reading: 'Building LLM-Powered Applications — Hugo Bowne-Anderson',
  listening: 'Tame Impala — Currents (on loop while shipping)',
  learning: 'Evals for agentic systems — keeping the loop honest.',
};
